# Shared Yes/No 源码开放准备

> 状态：**preparing / 私有首版源码准备中**，2026-09-24。由原开放计划整理为发布副本状态说明。不是公开发布完成声明，也不是干净机器复现验收。

## 已准备的本地副本

- 保留 Apache-2.0、上游归属、原 SDK 元数据与完整 `laya/` Python 包。
- 精选 S0/S1 builder、监督 trainer、评测器和实际导入依赖；保留 legacy helper 的结构，不重写训练框架。
- 增补[准备契约](data_and_models.md)、[canonical schema](SCHEMA.md)、[实际 CLI](reproduce.md)、[第三方表](../THIRD_PARTY.md)和[中文博客](blog_zh.md)。
- `bench_local.py` 仅增加可覆盖模型根目录的环境变量；其余训练损失、概率、指标和 batching 算法不改。
- 仅分发源码与两个配置 JSON（S1 配方、明确不可直接运行的 profile 模板）；不包含任何实验输出 JSON。

不复制旧私有路径下载器、缓存依赖的冻结 bundle 准备工具、notebooks、发布 workflows、数据、权重、预测、token IDs、原报告、环境文件或 Git/worktree 元数据。不改变外层工作树和原研究历史。

## 版本分界

原质量版本 `fb672477594f29681b90d13ec679819ab8033c5c` 配合 `full_test_v1_20260923_r2`，原 S1 训练版本 `80efa54951000a7b6f1637ea2bf146332540f9e2`，当前精选基线/批处理版本 `5d1b7de221579af74bde3131bc4eb8e61bba7c0f`。这些是来源标识，新仓库不含相应历史对象，不提供无效 checkout 命令。

首版未复制历史质量快照或 bundle；原质量数据表是研究汇总，不代表本副本运行结果。profile 不可静默指向最新批处理代码并称其为冻结版。

## 未完成及后续阶段

1. **资源与权利**：维护者确认新增源码权属、第三方通知、模型 custom code/stub 的版本和合法供应方式；逐来源处理数据再分发许可冲突。
2. **数据与模型单独发布**：目前没有文件，没有全部再分发授权；prepared 导出工具、完整官方来源版本与验证材料仍需补齐，不能借代码 Apache 统一重标数据/权重。
3. **独立执行验证**：在未来经授权的干净执行机上先 smoke，再检查正式构建配额与全部评测计数；没有隐藏 HOME 缓存后才能给出相应复现声明。
4. **人工发布审核**：维护者复核内容与历史；父流程负责新仓库 Git 操作及私有首提交/推送。本准备阶段没有暂存、提交、添加 remote 或推送，也没有公开发布。

源码副本并非安全审计证书。模式扫描不能穷尽秘密和版权风险；原实验输出即使叫 summary 也可能包含 prompt/token IDs，不能整包加入源码仓库。质量继续报告 batch 1；batch 32 性能与 BF16 数值差异单列。训练来源隔离不证明广泛语义新颖性或基础预训练零污染。
